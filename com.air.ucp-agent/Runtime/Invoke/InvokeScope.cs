namespace Air.UcpAgent.Invoke
{
    /// <summary>Declares which connector host kind can execute a command.</summary>
    public enum CommandHostScope
    {
        Editor,
        Runtime,
        Any,
    }
}
